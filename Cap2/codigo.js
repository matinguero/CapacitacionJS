
// ARRAYS ASOC.
/*
let pc1 = {
    nombre: "daltopc",
    procesador: "intel corei7",
    ram:"16GB",
    espacio: "1TB"
};
let nombre = pc1["nombre"];
let procesador = pc1["procesador"];
let ram = pc1["ram"];
let espacio = pc1["espacio"];

frase = `
    el nombre de mi PC es: <b>${nombre}</b> <br>
    el procesador es: <b>${procesador}</b> <br>
    la memoria ram es: <b>${ram}</b> <br>
    el espacio en disco es de <b>${espacio}</b>`;

document.write(frase);
*/
/*
for(let i = 0; i<6; i++){
    document.write(i + "<br>")
    //continue - Corta ESTA iteracion del for pero sigue con las otras

    //break - Rompe todo el bucle y finaliza el for


}
    */
 /*
let animales=["gato", "perro", "tiranosaurio rex"]
animales.edad=20;

for (animal in animales){
    document.write(animal + "<br>");
}

document.write("<br>");

for(animal of animales){
    document.write(animal + "<br>")
}

array1 = ["maría", "josefa", "roberta"];
array2 = ["pedro", "marcelo", array1, "josefina"];

forRandom:
for (let array in array2) {
    if (array == 2) {
        for (let array of array1) {
            if (array == "maría") {
                continue forRandom;
            }
            document.write(array + "<br>");
        }
    } else {
        document.write(array2[array] + "<br>");
    }
}
    */

/*
   function Saludar(){
    respuesta = prompt("hola");
    if(respuesta == "hola"){
        alert("hola");
        return "se ejecuto hola";
    }else{
        alert("chau");
        return "se ejecuto chau";
    }

   }

   
document.write(Saludar);

document.write(Saludar());

*/

